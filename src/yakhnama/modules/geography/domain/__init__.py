"""The ``domain`` layer of the ``geography`` module.

The ``Place`` aggregate (``entities``), administrative levels, codes, names and
geometry (``value_objects``), domain events, errors, the ``PlaceFactory`` and the
schema of the place reference file (``reference``). There are no authorisation
policies yet: they arrive with the identity module in Phase 2.

Patterns: Value Object, Entity, Aggregate Root, Domain Events, Factory, Domain Error.
"""
