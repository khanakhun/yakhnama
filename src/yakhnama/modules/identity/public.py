"""Public facade of the ``identity`` module.

Other modules import only this file: the ``Actor`` value object, the policy protocol and
the composable policies. Nothing is exported until the module has content.

Patterns: Facade.
"""
