"""The ``identity`` bounded context.

Owns the users mirrored from the OIDC provider, organisations, memberships, roles and
the authorisation policies every other module consults.

Patterns: Facade (see ``public.py``).
"""
