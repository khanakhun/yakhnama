"""The ``ingestion`` bounded context.

External dataset framework: catalog, versions, runs with lineage, the Template
Method pipeline, observations and STAC-aligned raster assets.

Patterns: Facade (see ``public.py``).
"""
