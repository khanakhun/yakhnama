"""The ``application`` layer of the ``ingestion`` module.

Commands, queries, handlers, ports and DTOs.

Patterns: Command Handler, Query Service, Repository (port side), Template Method,
Strategy, Registry.
"""
